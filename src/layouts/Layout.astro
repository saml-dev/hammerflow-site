---
import '../styles/global.css';
import background from '../assets/bg.webp';
import github from '../assets/github.svg';
import metaImage from '../assets/meta.png';
let aSite = Astro.site!;
aSite.pathname = metaImage.src;
let metaImageUrl = import.meta.env.PROD ? aSite.toString() : metaImage.src;
---

<!doctype html>
<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta
      name='viewport'
      content='width=device-width'
    />
    <link
      rel='icon'
      href='https://fav.farm/⚒️'
    />
    <meta
      name='generator'
      content={Astro.generator}
    />

    <!-- Open Graph tags -->
    <meta
      property='og:title'
      content='The most productive way to control your Mac'
    />
    <meta
      property='og:type'
      content='website'
    />
    <meta
      property='og:url'
      content='https://hammerflow.dev/'
    />
    <meta
      property='og:description'
      content='The most productive way to control your Mac'
    />
    <meta
      property='og:image'
      content={metaImageUrl}
    />
    <meta
      property='og:image:alt'
      content='Hammerflow'
    />

    <!-- Twitter -->
    <meta
      name='twitter:card'
      content='summary_large_image'
    />
    <meta
      name='twitter:title'
      content='Hammerflow'
    />
    <meta
      name='twitter:description'
      content='The most productive way to control your Mac'
    />
    <meta
      name='twitter:image'
      content={metaImageUrl}
    />

    <title>{import.meta.env.PROD ? 'Hammerflow' : 'Local Hammerflow'}</title>
  </head>
  <body class='text-gray-900'>
    <nav class='pt-4'>
      <div
        class='text-lg md:text-xl font-medium max-w-5xl px-4 md:px-8 mx-auto flex items-baseline justify-between'
      >
        <a href='/'>Hammerflow</a>
        <a
          target='_blank'
          href='https://github.com/saml-dev/Hammerflow.spoon.git'
          ><img
            class='size-6'
            src={github.src}
            alt='github logo'
          />
        </a>
      </div>
    </nav>
    <div class='max-w-5xl pb-40 px-4 md:px-8 mx-auto'>
      <slot />
    </div>
    <img
      id='background'
      src={background.src}
      alt=''
      fetchpriority='high'
    />
  </body>
</html>

<style>
  body {
    font-family: -apple-system, BlinkMacSystemFont, sans-serif;
    position: relative;
  }

  #background {
    position: absolute;
    inset: 0;
    opacity: 80%;
    width: 100%;
    height: 100%;
    z-index: -1;
  }
</style>
